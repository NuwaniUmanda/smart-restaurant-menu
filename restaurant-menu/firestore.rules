rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to menuItems collection
    match /menuItems/{document} {
      allow read, write: if true; // For development - change this for production
    }
  }
} 